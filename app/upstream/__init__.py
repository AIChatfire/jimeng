#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""上游适配层。

一个上游一个子包。当前只有 `jimeng/`。

**刻意不做"引擎 + 每上游一个脚本"那层抽象**（那属于 `image-adapter` 那种
"加一个上游只加配置"的多上游引擎）。本服务只服务即梦一个上游，
多一层抽象只会让「翻译逻辑到底在哪」变模糊 —— 上游 ≤2 且不常加时，单上游手写服务更省事。
真要接第二个上游，那时再引入引擎，而不是现在先把坑挖好。
"""
